import type { Metadata } from 'next'

export const metadata: Metadata = {
  title: 'Reset Password | CounsConnect',
}

export default function AuthLayout({ children }: { children: React.ReactNode }) {
  return children
}
